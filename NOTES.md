# NOTES — FatLabBromelainPatchLandingWidget

## Загальне

- **Префікс:** `flb` (класи `flb-`, змінні `--flb-`, функція `flb-fluid()`,
  ключі `FLB_`).
- **Ширина макета:** десктоп — 1440px, мобільний — 393px.
- **Точка перемикання:** одна, 700px (`max-width: 43.74875em`).
- **Шрифт:** Inter 400/500/600 (700 — для заголовків h2 і H1 hero), як у макеті
  (узгоджений виняток із README п. 4.5 «лише Gilroy» — дизайнери підтвердили, що
  це не принципово). Файли `fonts/inter-*.woff2` (латиниця + кирилиця),
  `@font-face` у `index.scss`; запасний — системний sans-serif (Gilroy у віджеті
  не використовується).

## Секції

1. Hero — фото товару, заголовок, підзаголовок, опис, факти з іконками, кнопка
   «Купити» (лише десктоп).
2. «Гладкість починається з мікроциркуляції» — фото моделі, три плашки переваг,
   декор (листок, молекула).
3. «Тепло, яке працює на результат» — порівняння до/після, дві плашки з
   показниками (лічильники), примітка.
4. «Більше гладкості — і це помітно» — чотири показники (лічильники),
   композиція: темна форма, листок, патч з упаковкою, логотип.
5. «Отримай більше, ніж просто warming-ефект» — п'ять сот-інгредієнтів,
   дах-градієнт, патч, камені, листок.
6. «Обирай зону» — фото, картка «Важливо!», чотири пульсуючі точки (лише CSS),
   чотири зони.
7. «Наклей — і дозволь патчу працювати» + «Як часто використовувати?» — три
   кроки, графік із 7 днів.
8. Фінальний блок — заголовок, переваги, опис набору, упаковка, кнопка «Купити»
   (лише десктоп).

## Інтерактив і анімації (за «Landing Page Animations»)

- Поява блоків при скролі: `opacity` + `transform`, перехід `.2s ease`, товар у
  hero виїжджає знизу на 50px; по черзі з'являються плашки переваг (секція 2),
  числові показники (секції 3 і 4), зони (секція 6), кроки і дні графіка (секція
  7). Соти інгредієнтів (секція 5) з'являються одночасно, без черги.
- Паралакс декоративних елементів (листки, молекули) по скролу і по курсору
  (`hooks/useParallax.ts`). Курсор — лише на пристроях з наведенням.
- Лічильники від 0 до значення в секціях 3 і 4 (`components/CountUp.tsx`),
  формат береться з тексту перекладу.
- Пульсація точок у секції 6 — CSS-анімація.
- Hover кнопки — підйом і тінь.
- `prefers-reduced-motion`: анімації, паралакс і лічильники вимикаються.
- Керування з клавіатури: інтерактивний елемент один — кнопка «Купити» (Tab,
  Enter, `:focus-visible`).

## Що не зверстано і чому

- Кнопка «Купити» на мобільному відсутня: за README (п. 4.2, 4.8) на мобільному
  використовується «В кошик» сайту, у мобільному макеті кнопки теж немає.

## Відхилення від макета

- Для секції 7 використано патерн іконок (`safety-pattern-*`), мобільну версію
  патерну секції 5 адаптовано з десктопної.
- Декоративні елементи секцій 4, 5 і 7 — темну фігуру, градієнтний елемент,
  шеврон і шестикутні комірки — відтворено у SVG відповідно до макета.
- Підпис кнопки береться з ключа сайту `ADD_TO_CART_PRODUCT_CARD` (у пісочниці
  «Add to cart», на сайті «Купити»).
- Кілька переносів рядків задано символом `\n` у `copy.ts` разом з
  `white-space: pre-line`, щоб текст переносився в потрібному місці (а не як
  вийде за шириною блока). При перекладі мовами, довшими за українську, ці
  переноси варто розставити заново під новий текст, а не залишати позицію з
  оригіналу:
  - Секція 1: `FLB_HERO_INSTRUCTION`.
  - Секція 3: `FLB_HEAT_NOTE`.
  - Секція 4: `FLB_RESULTS_TITLE`, `FLB_RESULTS_DESCRIPTION`,
    `FLB_RESULTS_STAT_1_LABEL` … `FLB_RESULTS_STAT_4_LABEL`, `FLB_RESULTS_NOTE`.
  - Секція 6: `FLB_ZONES_TITLE`, `FLB_ZONES_DESCRIPTION`,
    `FLB_SAFETY_DESCRIPTION`.
  - Секція 7: `FLB_USAGE_STEP_3_DESCRIPTION`.
- Планшетні ширини (700–1024px): через відсутність окремого планшетного макета
  розміри тексту в окремих компактних елементах зменшено нижче 12px, щоб
  зберегти композицію та уникнути накладань.

## Планшети (700–1024px)

Окремого планшетного макета немає, діє одна точка перемикання 700px (README, п.
4.1): десктопна композиція масштабується через `cqi`. Щоб текст не наїжджав на
сусідні блоки на ширинах 700–900px (iPad mini 768px), нижні межі десктопних
`clamp()` для шрифтів знижено: звичайний текст ≈ на 20% (не менше 11px), великі
заголовки ≈ на 30%; у картках секції 6 і кроках секції 7 дрібний текст —
9,5–10px. Це свідоме відхилення від правила «не менше 12px» (п. 4.3). У сотах
секції 5 на планшеті текст ще менший (заголовок від 9px, опис від 8,5px), щоб
уміщувався в соту. Верхні межі та значення `cqi` не змінено, тому на 1275px і
ширше вигляд той самий.

## Мобільна верстка

Мобільні композиції побудовано на відсотках, `aspect-ratio` і звичайному потоці
(секції 1, 2, 3, 5, 7, 8); фіксовані `px` залишено лише для відступів, дрібних
іконок і `max-width` текстових блоків. Шрифти — через `flb-fluid()`, крім тексту
в сотах секції 5, де використано `clamp()` з `vw`, щоб він вміщувався на 320px.

## Картинки

Усі растрові картинки — webp (ретина 2×), декор і форми — легкі SVG без
вбудованих растрів. Загалом `assets/` ≈ 2,6 МБ; жоден файл не більший за 300 КБ.

## Перевірки

- **Автоматичні перевірки з README (п. 9), прогнано 2026-09-22, усі пройдено:**
  - `npm run typecheck` — `tsc --noEmit` без помилок (exit 0).
  - `npm run build` — `tsc --noEmit && vite build` без помилок; збірка:
    `dist/assets/index-*.css` 55,63 kB (gzip 9,76 kB), `dist/assets/index-*.js`
    237,51 kB (gzip 71,73 kB).
  - `npm run check:keys -- src/common/widgets/landings/FatLabBromelainPatchLandingWidget FLB`
    → `prefix FLB_: 96 keys in code, 96 keys in src/copy.ts` / `OK: keys match`.
  - `npm run check:text -- src/common/widgets/landings/FatLabBromelainPatchLandingWidget`
    →
    `OK: no hardcoded text found in JSX (heuristic — also review each .tsx by eye)`.
  - `grep -rniE "(^|[^a-z0-9])mb[-_]" src/common/widgets/landings/FatLabBromelainPatchLandingWidget`
    → нічого не знайдено (залишків префікса прикладу `mb-`/`mb_` немає).
- Скріншоти по секціях: `screenshots/1440-<N>-<назва>.png` (колонка 1275px при
  вікні 1440px) і `screenshots/390-<N>-<назва>.png` (вікно 390px), N = 1–8.
- Горизонтального скролу немає на ширинах 320, 360, 390, 430, 699, 700, 900,
  1024, 1280, 1440, 1920.
- Тексти ×2 (стрес-тест): мобільна версія тримається, на десктопі при подвоєнні
  можливі накладання в плашках і підписах графіка — це очікуваний ліміт
  фіксованої піксельної композиції.
- **Зони ризику для довших перекладів — секції 5 і 7.** Соти інгредієнтів
  (секція 5) і шестикутники графіка (секція 7) — це декоративна форма з
  фіксованим розміром (`position: absolute`, координати з макета), а не
  розтяжний блок: текст росте всередину форми, а не розсуває її. Поточний
  український текст уже сидить впритул до межі:
  - назва інгредієнта — до 11 символів (`ЗЕЛЕНИЙ ЧАЙ`), опис — до 89 символів
    (`FLB_INGREDIENTS_BROMELAIN_DESCRIPTION`);
  - підпис дня графіка — 14 символів (`День із патчем`), для нього навіть
    примусово вимкнено перенос рядка (`white-space: nowrap`), щоб уміститися в
    соту. Переклад, помітно довший за ці значення, у соту не влізе — знадобиться
    або ще зменшувати шрифт (нижче вже знижених меж для планшета, п.
    «Планшети»), або змінювати форму карток на прямокутні. Тому для цих двох
    місць варто заздалегідь узгодити з перекладачем ліміт символів, а не
    покладатися на те, що верстка сама підлаштується.
